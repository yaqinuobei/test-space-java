spring:
  application:
    name: sb-encrypt-demo

  cloud:
    nacos:
      config:
        server-addr: ${NACOS_META:100.76.11.220:30266}
        file-extension: yaml
        namespace: ${NACOS_NAMESPACE:test}
        group: ${NACOS_GROUP:DEFAULT_GROUP}
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:R00t@996}